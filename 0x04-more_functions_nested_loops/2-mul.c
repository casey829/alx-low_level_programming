#include "main.h"
#include <stdio.h>

/**
 * mul - function that multiplies two integers
 * @a: integer
 * @b: interger
 *
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	int m;

	m = a * b;
	return (m);
}
