#include "main.h"

/**
 * main - function that  prints its name
 * @argc: argc parameter
 * @argv: an array of a command is listed
 * Return: 0 for success
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
