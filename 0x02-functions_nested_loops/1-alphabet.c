#include "main.h"

/**
 * main - a function that prints the alphabet
 *
 * Return:Always  0 (Success)
 */
int num(void)
{
	int i = 'a';

	for(num = i; i <= 'z'; i++)
	{
		_putchar('%d', num);
		_putchar('\n');

		return (0);
	}
}
